Source generator for the Ease* curve algorithms in the tween/curves package.

The generated source is placed into `curves/ease.go`

The generator will be automatically run to regenerate ease.go from the root
directory:

`go generate ./...`
